/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
}

module.exports = {
    images: {
        domains: ['i.imgur.com', 'imgur.com', 'cdn.discordapp.com', 'media.discordapp.net', 'github.com', 'avatars0.githubusercontent.com']
    },
}
